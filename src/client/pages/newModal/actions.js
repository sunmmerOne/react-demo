// 这是自动生成的文件，可以修改。
import * as type from './constants'

const newModalAction = param => dispatch => {
    dispatch({
        type: type.NEWMODAL,
        data: {},
    });
};

export {
    newModalAction
};