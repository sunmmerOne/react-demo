// 这是自动生成的文件，可以修改。
import * as type from './constants'

const templateAction = param => dispatch => {
    dispatch({
        type: type.TEMPLATE,
        data: {},
    });
};

export {
    templateAction
};